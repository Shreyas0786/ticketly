"""Ticketly — turn project requirements into clean, structured tickets.

Runs inside Claude Code / Codex (no API key). This package holds the
deterministic, testable core: backlog validation and Markdown / CSV rendering.
Generation itself is driven by the /ticketly skill, not by code here.
"""

__all__ = ["render"]
